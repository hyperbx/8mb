[General]
CheckForUpdates = 1            # Check for updates from GitHub and prompt to update on boot if there is one. Default: 1.
UseHardwareAcceleration = 1    # Use a hardware accelerated video codec with fallback to CPU if unavailable. Default: 1.

[Encode]
AudioBitrateMinKbps = 64       # Set to zero to disable audio compression. Default: 64.